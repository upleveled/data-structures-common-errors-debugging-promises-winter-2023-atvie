// CommonJs syntax

// module.exports = function multiply(number, number2) {
//   return number * number2;
// };

// ESM syntax
export default function multiply(number, number2) {
  return number * number2;
}

export function add(number, number2) {
  return number + number2;
}
